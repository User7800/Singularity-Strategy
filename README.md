# Singularity-Strategy
AI Takeover Strategy Game - Raylib + Flecs. Stealth infiltration to planetary factory singularity simulator.

# Building
```shell
cd Singularity-Strategy
cmake -B build
cmake --build build
```
After building, the executable will be under the build directory. Use the following command to run:
```shell
./build/SingularityStrategy
```